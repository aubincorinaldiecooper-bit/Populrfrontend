import NotAvailableYet from '../components/NotAvailableYet';

export default function CampaignBuilderPage() {
  return (
    <NotAvailableYet
      title="Campaign builder"
      icon="campaigns"
      description="Multi-step campaigns aren't built yet. To set up a keyword-triggered reply or DM today, create an automation."
    />
  );
}
