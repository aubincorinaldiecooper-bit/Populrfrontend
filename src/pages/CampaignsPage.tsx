import NotAvailableYet from '../components/NotAvailableYet';

export default function CampaignsPage() {
  return (
    <NotAvailableYet
      title="Campaigns"
      icon="campaigns"
      description="Multi-step campaigns aren't built yet. Keyword-triggered replies and DMs are available today as automations."
    />
  );
}
