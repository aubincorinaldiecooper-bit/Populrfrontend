import NotAvailableYet from '../components/NotAvailableYet';

export default function SegmentsPage() {
  return (
    <NotAvailableYet
      title="Segments"
      icon="contacts"
      description="Saved audience segments aren't built yet. You can filter your audience by stage and search it on the Contacts page."
    />
  );
}
