import NotAvailableYet from '../components/NotAvailableYet';

export default function AnalyticsPage() {
  return (
    <NotAvailableYet
      title="Analytics"
      icon="analytics"
      description="Cross-account reporting isn't built yet. Each automation's own activity log — what it replied to and when — is on its detail page under Automations."
    />
  );
}
