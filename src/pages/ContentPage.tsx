import NotAvailableYet from '../components/NotAvailableYet';

export default function ContentPage() {
  return (
    <NotAvailableYet
      title="Content"
      icon="content"
      description="A library of your published posts and what each one earned isn't built yet. The posts an automation can watch are shown while you're setting one up."
    />
  );
}
